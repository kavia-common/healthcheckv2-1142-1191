# Example environment variables for healthcheckv2
# HEALTHCHECK_CONFIG can override config file location (default: /app/config/config.yaml)
HEALTHCHECK_CONFIG=/app/config/config.yaml
HEALTHCHECK_ENV=dev
